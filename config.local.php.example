<?php
// ============================================================
// config.local.php — Production overrides
// ============================================================
// Copy file này thành config.local.php trên server.
// File KHÔNG bị commit vào git và KHÔNG bị ghi đè khi update.
//
// Chỉ khai báo key muốn ghi đè — phần còn lại lấy từ config.php.
// ============================================================

return [
    // Bật tính năng đăng nhập (dùng user đã có trong database)
    'auth' => [
        'enabled' => true,
    ],

    // Ghi đè DB password nếu cần
    // 'db' => [
    //     'password' => 'your_db_password',
    // ],
];
