# ── Sensitive / Environment ────────────────────────────────────────────────
config.php
config.local.php
.env
.env.*
.installed

# ── Uploads & user data ────────────────────────────────────────────────────
uploads/*
!uploads/.htaccess

# ── Build artifacts ────────────────────────────────────────────────────────
*.zip
*.log

# ── Dependencies (exclude — run composer install on host or use build.sh) ──
vendor/

# ── Dev / planning files (now in others/) ─────────────────────────────────
# others/ is tracked in git but excluded from build zip via build.sh

# ── OS ─────────────────────────────────────────────────────────────────────
.DS_Store
__MACOSX/
Thumbs.db

# ── IDE & local tools ──────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
.claude/
