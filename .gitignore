# ── Sensitive / Environment ────────────────────────────────────────────────
config.php
config.local.php
.env
.env.*
.installed

# ── Uploads & user data ────────────────────────────────────────────────────
uploads/*
!uploads/.htaccess

# ── Build artifacts ────────────────────────────────────────────────────────
*.zip
*.log
!release/*.zip

# ── Dependencies (exclude — run composer install on host or use build.sh) ──
vendor/

# ── Dev / planning files ───────────────────────────────────────────────────
others/

# ── OS ─────────────────────────────────────────────────────────────────────
.DS_Store
__MACOSX/
Thumbs.db

# ── IDE & local tools ──────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
.claude/
